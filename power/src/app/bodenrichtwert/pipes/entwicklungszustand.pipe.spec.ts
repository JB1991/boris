import { EntwicklungszustandPipe } from './entwicklungszustand.pipe';

describe('EntwicklungszustandPipe', () => {
  it('create an instance', () => {
    const pipe = new EntwicklungszustandPipe();
    expect(pipe).toBeTruthy();
  });
});
