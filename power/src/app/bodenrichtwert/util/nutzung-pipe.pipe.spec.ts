import { NutzungPipePipe } from './nutzung-pipe.pipe';

describe('NutzungPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NutzungPipePipe();
    expect(pipe).toBeTruthy();
  });
});
