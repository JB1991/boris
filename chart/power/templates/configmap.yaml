apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "power.fullname" . }}-nginx
  labels:
    {{- include "power.labels" . | nindent 4 }}
data:
  power.conf: |-
    {{ tpl (.Files.Get "power.conf") . | quote }}
  nginx.conf: |-
    {{ tpl (.Files.Get "nginx.conf") . | quote }}
